# ============================================================
# CRD: SampleResource 自定义资源定义（代码生成输出）
# ============================================================
#
# 此文件由 controller-gen 工具自动生成
# 手动修改可能会被覆盖
#
# 【使用方式】
# kubectl apply -f config/crd/sample-crd.yaml

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: sampleresources.sample.ai-infra
spec:
  group: sample.ai-infra
  names:
    kind: SampleResource
    listKind: SampleResourceList
    plural: sampleresources
    singular: sampleresource
    shortNames:
      - sr
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - replicas
                - feature
              properties:
                replicas:
                  type: integer
                  minimum: 1
                  maximum: 100
                feature:
                  type: string
                  enum:
                    - advanced-scheduling
                    - basic
                    - none
                image:
                  type: string
                port:
                  type: integer
            status:
              type: object
              properties:
                phase:
                  type: string
                replicas:
                  type: integer
                availableReplicas:
                  type: integer
                observedGeneration:
                  type: integer
                conditions:
                  type: array
                  items:
                    type: object
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                      reason:
                        type: string
                      message:
                        type: string
                      lastTransitionTime:
                        type: string
                        format: date-time
                      observedGeneration:
                        type: integer
      additionalPrinterColumns:
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Replicas
          type: integer
          jsonPath: .spec.replicas
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      subresources:
        status: {}
